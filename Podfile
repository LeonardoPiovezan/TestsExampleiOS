# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestsExamples' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestsExamples

  target 'TestsExamplesTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick'
    pod 'Nimble'
    pod 'KIF'
    pod 'iOSSnapshotTestCase'
  end

  target 'TestsExamplesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
